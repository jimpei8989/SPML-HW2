recorder_root: ${logs/pyramidnet110_a48-adversarial-training}
model_name: pyramidnet110_a48
attack:
  method: pgd
  num_iters: 10
  alpha: 0.015625
optimizer:
  name: SGD
  lr: 0.0005
  scheduler:
    name: CyclicLR
    type: batch
    base_lr: 0.00001
    max_lr: 0.0005
misc:
  num_epochs: 500
  batch_size: 128
  num_workers: 8
  checkpoint_period: 10
  adversarial_examples_resample_period: 20
